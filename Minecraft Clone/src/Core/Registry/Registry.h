#include <unordered_map>
#include <string>
#include <exception>

template <class T> class Registry {
public:
	T GetValue(std::string Value, ) {

	}

private:
	std::unordered_map<std::string, T> Hashmap;
};